apiVersion: v1
kind: Service
metadata:
  name: hordeforge
  labels:
    app: hordeforge
spec:
  type: ClusterIP
  selector:
    app: hordeforge
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP

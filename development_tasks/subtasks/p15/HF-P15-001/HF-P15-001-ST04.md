# HF-P15-001-ST04 — Добавить шаг rag_initializer в pipeline

- Parent Task: HF-P15-001
- Parent Title: Lightweight Repo RAG Core
- Phase: P15
- Priority: P0
- Estimate: 0.5d
- Dependencies: HF-P15-001-ST01, HF-P15-001-ST02, HF-P15-001-ST03

## Цель

Интегрировать RAG в pipeline: создать шаг rag_initializer для индексации репозитория.

## BDD постановка

### Feature

HF-P15-001 Lightweight Repo RAG Core

### Scenario

HF-P15-001-ST04 Интеграция rag_initializer в feature_pipeline

Given настроенный feature_pipeline с шагами
When pipeline выполняется с параметром `repo_url`
Then шаг rag_initializer выполняет:
1. Скачивает/читает репозиторий
2. Извлекает symbols через AST
3. Создает embeddings для chunks
4. Индексирует в Qdrant
And возвращает artifact `rag_index` с metadata
And downstream агенты могут использовать RAG

## TDD постановка

### Red

1. Добавить тест вызова rag_initializer в pipeline.
2. Ожидаемое:
   - artifact `rag_index` содержит: index_id, documents_count, collections
   - artifact имеет статус SUCCESS

### Green

1. Обновить `agents/rag_initializer.py`:
   - Добавить логику индексации репозитория
   - Использовать symbol_extractor и hybrid_retriever
   - Создавать chunks для каждого symbol
2. Обновить YAML pipeline если нужно

### Refactor

1. Добавить incremental indexing (только измененные файлы)
2. Кеширование результатов
3. Обработка больших репозиториев (пагинация)

## Тест-дизайн

```python
def test_rag_initializer_indexes_repo():
    # Arrange
    agent = RagInitializer()
    context = {
        "repo_url": "https://github.com/acme/repo.git",
        "github_token": "test_token"
    }
    
    # Act
    result = agent.run(context)
    
    # Assert
    assert result["status"] == "SUCCESS"
    rag_index = result["artifacts"][0]["content"]
    assert "index_id" in rag_index
    assert rag_index["documents_count"] > 0
    assert "collections" in rag_index
```

## Definition of Done

- [ ] RagInitializer интегрирует symbol_extractor
- [ ] RagInitializer создает chunks и embeddings
- [ ] Индексация в Qdrant работает
- [ ] artifact `rag_index` возвращается с metadata
- [ ] Unit тесты проходят
- [ ] Интеграционный тест с реальным репозиторием

## Артефакты

- **кодовые изменения:**
  - `agents/rag_initializer.py` — обновление: добавление full indexing логики

- **тесты:**
  - `tests/unit/agents/test_rag_initializer.py` — обновление: расширение тестов

- **docs:**
  - `docs/indexing.md` — обновление: добавление incremental indexing секции

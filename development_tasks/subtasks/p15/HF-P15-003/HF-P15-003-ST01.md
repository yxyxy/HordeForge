# HF-P15-003-ST01 — Обновить pipeline с шагами memory

- Parent Task: HF-P15-003
- Parent Title: Pipeline Integration & Optimization
- Phase: P15
- Priority: P1
- Estimate: 0.5d
- Dependencies: HF-P15-001-ST04, HF-P15-002-ST02, HF-P15-002-ST03

## Цель

Создать code_generation pipeline с полной интеграцией memory.

## BDD постановка

### Feature

HF-P15-003 Pipeline Integration & Optimization

### Scenario

HF-P15-003-ST01 Выполнение code_generation pipeline с memory

Given настроенный feature_pipeline
When запускается pipeline с issue "Add user authentication"
Then выполняются шаги:
1. rag_initializer - индексация репозитория
2. memory_retrieval - поиск похожих задач
3. planner - создание плана с memory context
4. code_generator - генерация кода с memory context
5. review - ревью кода
6. memory_writer - сохранение результата в memory

## TDD постановка

### Red

1. Добавить тест запуска pipeline.
2. Ожидаемое:
   - Все шаги выполняются без ошибок
   - memory_retrieval возвращает результаты
   - memory_writer вызывается после review

### Green

1. Создать `pipelines/code_generation.yaml`
2. Добавить шаги:
   - memory_retrieval (новый агент)
   - memory_writer (использует hook)
3. Обновить feature_pipeline.yaml

### Refactor

1. Убрать дублирование между pipelines
2. Вынести общие шаги в отдельный YAML include
3. Добавить conditional steps

## Тест-дизайн

```python
def test_code_generation_pipeline_with_memory():
    # Arrange
    engine = OrchestratorEngine()
    
    # Act
    result = engine.run(
        "code_generation",
        {"issue": {"title": "Add auth", "body": "..."}},
        run_id="test-001"
    )
    
    # Assert
    assert result["status"] in {"SUCCESS", "PARTIAL_SUCCESS"}
    assert "memory_retrieval" in result["steps"]
    assert "memory_writer" in result["steps"]
    # Verify memory entries were created
    memory_store = get_memory_store()
    assert memory_store.count() > 0
```

## Definition of Done

- [ ] code_generation.yaml pipeline файл
- [ ] Шаг memory_retrieval интегрирован
- [ ] Шаг memory_writer работает после review
- [ ] Интеграционный тест pipeline
- [ ] E2E тест с реальным GitHub репозиторием

## Артефакты

- **кодовые изменения:**
  - `pipelines/code_generation.yaml` — новый: полный pipeline с memory
  - `pipelines/feature_pipeline.yaml` — обновление: добавление memory шагов

- **тесты:**
  - `tests/integration/test_pipeline_with_memory.py` — новый

- **docs:**
  - `docs/pipeline_memory_flow.md` — новая: схема flow с memory

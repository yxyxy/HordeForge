# HF-P15-002-ST01 — Создать структуру payload для memory entries

- Parent Task: HF-P15-002
- Parent Title: Agent Memory System
- Phase: P15
- Priority: P1
- Estimate: 0.5d
- Dependencies: HF-P15-001-ST01, HF-P15-001-ST04

## Цель

Определить схемы данных для хранения памяти агентов в Qdrant коллекциях.

## BDD постановка

### Feature

HF-P15-002 Agent Memory System

### Scenario

HF-P15-002-ST01 Определение структуры memory payload

Given требования к памяти агентов
When разработчик создает memory entry
Then payload содержит обязательные поля:
- id (UUID)
- type (task | patch | decision)
- task_description
- agents_used
- result_status
- timestamp
- И type-specific поля (diff, file, reason и т.д.)

## TDD постановка

### Red

1. Добавить тест валидации payload.
2. Ожидаемое:
   - Все обязательные поля присутствуют
   - type-specific валидация работает
   - payload сериализуется в JSON

### Green

1. Создать `rag/memory_collections.py`
2. Определить dataclasses:
   - MemoryEntry (base)
   - TaskEntry (type="task")
   - PatchEntry (type="patch")
   - DecisionEntry (type="decision")
3. Создать factory функцию для создания entries

### Refactor

1. Добавить версионирование схемы
2. Добавить миграции для существующих entries
3. Оптимизировать размер payload

## Тест-дизайн

```python
def test_task_entry_schema():
    # Arrange & Act
    entry = TaskEntry(
        task_description="Fix authentication bug",
        pipeline="feature_pipeline",
        agents_used=["planner", "code_generator", "review"],
        result_status="MERGED"
    )
    
    # Assert
    assert entry.type == "task"
    assert entry.task_description == "Fix authentication bug"
    assert len(entry.agents_used) == 3
    
def test_patch_entry_schema():
    # Act
    entry = PatchEntry(
        task_description="Fix auth bug",
        file="auth.py",
        diff="--- a/auth.py\n+++ b/auth.py",
        reason="null check missing"
    )
    
    # Assert
    assert entry.type == "patch"
    assert entry.file == "auth.py"
    assert entry.diff.startswith("---")
```

## Definition of Done

- [ ] MemoryEntry base dataclass
- [ ] TaskEntry, PatchEntry, DecisionEntry dataclasses
- [ ] Factory функция create_memory_entry()
- [ ] Валидация полей
- [ ] JSON сериализация/десериализация
- [ ] Unit тесты

## Артефакты

- **кодовые изменения:**
  - `rag/memory_collections.py` — новый: схемы memory entries

- **тесты:**
  - `tests/unit/rag/test_memory_collections.py` — новый

- **docs:**
  - `docs/memory_collections.md` — новая: схемы данных memory

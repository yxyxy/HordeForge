# Coding Standards

## Reliability
Prefer deterministic logic, explicit error handling, and stable interfaces for runtime components.

## Testing
For every behavior change, add regression tests and validate linting and formatting gates.

## Security
Never store secrets in plain text and ensure payloads are sanitized before persistence.
